name: Build and Push Triceratops

on:
  push:
    branches: [ "main" ]
    tags: [ 'v*' ]
  pull_request:
    branches: [ "main" ]

env:
  REGISTRY: ghcr.io
  IMAGE_BASE: ghcr.io/pesegato/triceratops-base
  IMAGE_APP: ghcr.io/pesegato/triceratops-app

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: gradle

      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # 1. Build & Push della Base Image (solo se il Dockerfile è cambiato)
      - name: Build and push Base Image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Dockerfile
          push: true
          tags: ${{ env.REGISTRY }}/${{ github.repository }}-base:latest

      # 2. Build & Push dell'App tramite Jib
      - name: Build and push App with Jib
        run: |
          ./gradlew jib \
            -Djib.from.image=${{ env.REGISTRY }}/${{ github.repository }}-base:latest \
            -Djib.to.image=${{ env.REGISTRY }}/${{ github.repository }}-app \
            -Djib.to.auth.username=${{ github.actor }} \
            -Djib.to.auth.password=${{ secrets.GITHUB_TOKEN }}